# Notas:

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Sera la base para crear la tabla de multiplicar
                                                            [número] [requerido]
  -n, --limite   Define el limite al que se creara la tabla
                                                          [número] [defecto: 10]
  -l, --listar   Mostrara la tabla creada en consola [booleano] [defecto: false]
```